{
  "name": "playground",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun run artillery run --output test.json test.yaml && bun artillery report --output test.html test.json",
    "show": ""
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "artillery": "^2.0.21"
  }
}
